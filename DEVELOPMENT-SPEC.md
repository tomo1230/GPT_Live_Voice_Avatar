# NEON VRM Motion Studio 開発仕様書

更新日: 2026-07-26  
バージョン: 0.1.0  
状態: VRM・VRMA配信基盤、人間化モーション、OBS支援機能の実装完了

最終ビルド確認: `npm run check` 成功  
実装エントリー: `src/vrm-app.js`  
配布用バンドル: `public/app.js`

## 1. 目的

VBCABLEなどのローカル音声入力に同期して動作する、完全ローカル型VRMバーチャルヒューマンを構築する。

本システムは会話AIではなく、入力音声を直接解析して以下を生成することを主目的とする。

- VRM標準表情を使った母音リップシンク
- 自動瞬き
- VRMAによる待機・会話・リアクションモーション
- 発話状態に応じたモーション選択とクロスフェード

音声認識、LLM、音声合成、外部APIは現在の実行経路では使用しない。

## 2. 基本要件

### 2.1 動作環境

- OS: Windows
- Node.js: 20以上
- ブラウザー: WebGL、Web Audio API、IndexedDB対応ブラウザー
- 待受アドレス: `127.0.0.1`
- ポート: `4782`
- URL: `http://127.0.0.1:4782`

### 2.2 プライバシー

- VRM・VRMAは外部へ送信しない
- 音声はブラウザー内のWeb Audio APIで解析する
- 録音、文字起こし、サーバー送信を行わない
- APIキー不要
- 読み込んだファイルはブラウザーのIndexedDBへローカル保存する

## 3. 技術構成

| 分類 | 採用技術 |
|---|---|
| 3D描画 | Three.js 0.185.1 |
| VRM | `@pixiv/three-vrm` 3.5.5 |
| VRMA | `@pixiv/three-vrm-animation` 3.5.5 |
| 音声解析 | Web Audio API |
| フロントエンド | HTML、CSS、JavaScript |
| バンドル | esbuild 0.28.1 |
| ローカルサーバー | Node.js HTTPサーバー |
| 設定保存 | IndexedDB、localStorage |

## 4. ディレクトリ構成

```text
GPT_Voice/
├─ public/
│  ├─ index.html              # UI
│  ├─ style.css               # ブルー・クラウドテーマ
│  └─ app.js                  # esbuild生成物
├─ src/
│  └─ vrm-app.js              # VRMアプリ本体
├─ motions/
│  └─ simple-generated/       # 簡易生成VRMA
├─ tools/
│  ├─ generate-simple-vrma.mjs
│  └─ fbx2vrma-converter/
├─ legacy-photo-avatar/       # 移行前の写真版
├─ server.mjs
├─ package.json
├─ start-neon-vrm.bat
└─ README.md
```

## 5. 起動仕様

### 5.1 通常起動

`start-neon-vrm.bat`をダブルクリックする。

バッチ処理:

1. Node.jsの存在確認
2. `node_modules`がなければ`npm install`
3. `127.0.0.1:4782`の起動状態を確認
4. 未起動の場合は最小化したコマンド画面で`npm start`
5. 最大20秒間サーバー応答を待機
6. 既定ブラウザーでURLを開く
7. 起動済みの場合は二重起動せずURLだけを開く

### 5.2 開発コマンド

```powershell
npm install
npm run build
npm run check
npm run generate:motions
npm start
```

## 6. VRM仕様

### 6.1 読み込み

- VRM 0.x、VRM 1.0対応
- 画面へのドラッグ＆ドロップ
- ファイル選択ダイアログ
- モデル交換対応
- ファイルはArrayBufferとしてブラウザー内で解析

### 6.2 表示

- WebGLアンチエイリアス有効
- 描画品質は自動、1.0x、1.5x、2.0xから選択可能
- 自動画質はFPSとCPU処理時間から1.0～2.0xを0.25刻みで調整
- 半球ライト、キーライト、リムライトを使用
- ソフト、明るい、ステージの照明プリセット
- モデルのBoundingBoxからカメラ位置を自動調整
- 全身、上半身、顔のカメラ構図プリセット
- 構図切り替えは0.55秒でイーズ補間
- OrbitControlsで回転・ズーム・注視点操作
- ブルーの半透明足元ライトを表示

### 6.3 表情

使用するVRM標準表情:

- `aa`
- `ih`
- `ou`
- `ee`
- `oh`
- `blink`
- `happy`
- `relaxed`
- `surprised`

VRMAやVRM更新の後に表情を最終適用し、VRMA内の表情トラックによるリップシンク上書きを防ぐ。

`LIP TEST`では約1.2秒間、`aa`を0.9で適用する。

通常の自動表情はHappy、Relaxed、OFF、強調表情はSurprised、Happy、OFFからモデルごとに選択する。

瞬きは135～210msで変化し、約18%の確率で二度瞬きを行う。視線移動の一部は瞬き中に実行する。

## 7. 音声入力・リップシンク仕様

### 7.1 入力

- `navigator.mediaDevices.getUserMedia`
- 入力デバイス選択対応
- VBCABLE利用可能
- エコーキャンセル無効
- ノイズ抑制無効
- 自動ゲイン無効
- 保存済みデバイスが消えた場合は既定入力へフォールバック
- デバイスの接続・切断を`devicechange`とMediaStreamTrackの`ended`で監視

ブラウザーのセキュリティ制約により、音声リンクは起動ごとに手動開始する。

### 7.2 解析

- `AnalyserNode.fftSize`: 1024
- `smoothingTimeConstant`: 0.48
- RMSからdBと発話ゲートを計算
- 開始後0.8秒で無音レベルを自動測定
- 使用中も環境ノイズへ低速追従
- F1・F2相当の周波数帯から母音重みを推定
- 主母音を指数1.5で強調し、口形の混濁を軽減
- 母音値は時間方向に平滑化
- 口の開閉は立ち上がりと戻りで異なる係数を使用
- 急な音量低下から子音相当の短い閉口を生成
- リップシンクは0～300msの遅延バッファに対応
- FFT波形配列と周波数配列は音声リンク開始時に一度だけ確保し、毎フレーム再利用

母音推定帯域:

| 値 | 周波数帯 |
|---|---|
| F1 Low | 180–380Hz |
| F1 Mid | 380–650Hz |
| F1 High | 650–1000Hz |
| F2 Low | 700–1250Hz |
| F2 High | 1900–3200Hz |

## 8. VRMA仕様

### 8.1 モーションスロット

| スロット | 用途 | 再生方式 |
|---|---|---|
| IDLE | 待機 | ループ |
| TALK A | 通常会話A | ループ |
| TALK B | 通常会話B | ループ |
| EMPHASIS | 強調会話 | ループ |
| LISTEN | 傾聴 | ループ |
| NOD | うなずき | 1回 |
| GREETING | 挨拶 | 1回 |

### 8.2 自動選択

- TALK AとTALK Bは発話ごとに交互選択
- 長い発話では約3.8～7秒ごとに、音量の落ち着いた位置で自動交代
- 発話開始後240ms以内に強い入力を検出した場合はEMPHASIS
- TALK BがなければTALK Aへフォールバック
- EMPHASISがなければ通常会話へフォールバック
- 音節間の短い無音は240ms保持
- IDLE復帰クロスフェード: 約0.62秒
- IDLEから会話: 約0.38秒
- TALK A/B間: 約0.50秒、再生位相を維持
- EMPHASIS: 約0.26秒
- LISTEN: 約0.50秒
- NOD/GREETING: 約0.34秒
- 切替時間はUIから0.5～2.0倍へ調整可能
- フェード完了後の旧アクションは自動停止
- ループごとにIdleは96.5～103.5%、Talkは98～102%の微小速度変化

### 8.3 手動再生

`ACTION PREVIEW`から以下を再生できる。

- LISTEN
- NOD
- GREETING
- LIP TEST

NODとGREETING終了後:

- 発話中なら選択中の会話モーションへ復帰
- 無音ならIDLEへ復帰

スロットUIにはクリップ時間、ループ/単発種別、現在再生中のハイライトを表示する。右クリックと確認操作で個別VRMAを解除できる。

### 8.4 VRMA未設定時

VRMAが一つも読み込まれていない場合のみ、コード生成の簡易全身モーションへフォールバックする。

### 8.5 人間化オーバーレイ

VRMAの姿勢を基礎に、以下を弱い加算処理として適用する。

- 腰の微小な左右重心移動
- 胸と肩の遅延追従
- 3.3～5.4秒で変化する不規則な呼吸
- 約9%の確率で少し深い呼吸
- カメラレンズ周辺のマイクロサッカード
- 発話中の視線活動量増加
- 音声の立ち上がりに反応する最大約0.5度の頭部うなずき
- 音量連動の微表情

加算した回転と位置は次フレーム冒頭で逆変換し、VRMAに対象トラックがない場合も姿勢が蓄積しないようにする。

「控えめ」「標準」「活発」のプリセット、および0～2倍の個別調整を提供する。

## 9. 簡易VRMAジェネレーター

### 9.1 出力

`tools/generate-simple-vrma.mjs`が以下を生成する。

```text
motions/simple-generated/
├─ Idle.vrma
├─ Talk-A.vrma
├─ Talk-B.vrma
├─ Emphasis.vrma
├─ Listen.vrma
├─ Nod.vrma
└─ Greeting.vrma
```

### 9.2 生成方式

- FBXを使用せずGLBバイナリを直接生成
- `VRMC_vrm_animation` 1.0拡張
- VRM標準Humanoidボーンをマッピング
- 回転はクォータニオン
- 30fpsへ再サンプリング
- Smoothstepによるイーズイン・アウト
- 肩、上腕、肘、手首の順に追従遅延を付与
- 左右30本の指ボーンを収録
- 手首から指先へ段階的な追従遅延を付与
- 待機時の微細な開閉、会話時の握り、強調・挨拶時の開いた手を生成
- クリップ先頭と末尾では遅延をゼロへ戻す
- ループ境界の姿勢を一致させる

Idleの腕は初期生成値から約15度下げた状態を基準とする。

### 9.3 高品質モーション

高品質なVRMAが必要な場合は、`tools/fbx2vrma-converter`を使用してMixamoなどのFBXを変換する。

```powershell
node fbx2vrma-converter.js -i input.fbx -o output.vrma
```

## 10. 設定保持

### 10.1 IndexedDB

データベース名: `neon-vrm-studio`  
オブジェクトストア名: `local-files`

保存キー:

- `model`
- `motion:idle`
- `motion:talk`
- `motion:talkAlt`
- `motion:emphasis`
- `motion:listen`
- `motion:nod`
- `motion:greeting`

保存内容:

- ファイル名
- MIMEタイプ
- ArrayBuffer

起動時はVRMを先に復元し、その後各VRMAを順番に復元する。

### 10.2 localStorage

キー: `neon-settings`

保存内容:

- 口の感度
- 口の遅延
- 動作の大きさ
- 自然な動き
- 表情の強さ
- 視線の動き
- うなずき
- VRMA再生速度
- クロスフェード倍率
- 背景モード
- 描画品質
- 自動表情割り当て
- 照明プリセット
- 入力デバイスID

キー`neon-model-profiles`には、VRMファイル名ごとの上記調整値、カメラ位置、カメラ注視点を保存する。

設定と全モデルプロファイルはNEON形式のJSONとして書き出し・読み込みできる。VRM・VRMA本体はJSONに含めない。

NEON環境パック（`.neonpack`）は、設定、全モデルプロファイル、保存中のVRM、全VRMAを独自バイナリ形式で一括移行する。ヘッダーと全データ長を検証し、IndexedDBへの置換は単一トランザクションで行う。

## 11. UIデザイン

- テーマ: GTPLive系ブルー・クラウドグラデーション
- 複数のラジアルグラデーションで雲状の霞を表現
- 操作パネルは半透明ダークブルー
- `backdrop-filter`によるガラス表現
- シアンのアクセントと発光
- WebGLキャンバスは透過
- 背景はGTPLiveブルー、透明、クロマキー緑から選択
- 配信表示ではヘッダー、サイドパネル、フッター、ステータスを隠してCanvasを全画面化
- 配信表示はEscまたはダブルクリックで解除
- OBS用URLパラメーターは`broadcast=1`、`ui=0`、`background=blue|transparent|green`、`quality=auto|low|standard|high`
- URL指定の背景・品質はセッション限定で、localStorageの通常設定を上書きしない
- 狭い画面では1カラムへ変更

フッターにはFPS、平均フレーム間隔、CPU処理時間、三角形数、描画倍率、口遅延を表示する。

## 12. ビルド

エントリーポイント:

```text
src/vrm-app.js
```

出力:

```text
public/app.js
```

ビルド設定:

```text
--bundle
--format=esm
--platform=browser
--target=es2022
```

## 13. 既知の制約

- VRMに標準母音表情がない場合、リップシンクできない
- VRMAの品質は元モーションまたは生成キーフレームに依存する
- モデルごとの骨軸差により、簡易生成VRMAの腕・手首角度に調整が必要な場合がある
- 簡易生成VRMAにはIK、足接地補正、衝突回避がない
- 音声のみから正確な日本語母音を識別する方式ではない
- マイクは自動開始できない
- RDPセッションでは対象PC内のVB-CABLEがブラウザーへ公開されない場合がある
- IndexedDBを削除すると保存済みVRM・VRMAも消える
- モデルプロファイルはファイル名を識別キーにするため、同名の別モデルは設定を共有する
- 汎用の股関節・膝回転IKは骨軸差で脚を崩すため無効。接地は足裏高を考慮した腰Y補正のみ
- 写真版は開発対象外として`legacy-photo-avatar`へ保存済み

## 14. 今後の拡張候補

### 14.1 未実装

優先度順:

1. 保存済みVRM・VRMAの一括管理画面
   - IndexedDB内のファイル一覧
   - ファイルサイズ表示
   - 個別削除
   - 全データ消去
2. モーションスロット別の再生速度・フェード設定
   - 現在は全スロット共通設定
   - Idle、Talk、単発モーションごとの個別値は未対応
3. GPU計測を含む詳細パフォーマンス診断
   - 現在はFPS、フレーム間隔、JavaScript側CPU時間、三角形数を表示
   - WebGL Timer Queryを使ったGPU時間は未対応
4. モデル固有の膝軸を考慮した任意IK
   - 汎用回転IKは脚を大きく崩したため撤回済み
   - 実装する場合はモデルごとの膝方向、脚長、足裏オフセットが必要
5. 自動テスト
   - VRM・VRMA読み込み
   - IndexedDB復元
   - 環境パック往復
   - URLパラメーター
   - 設定プロファイル切り替え

### 14.2 実機で残る確認

- 対象PCのローカルセッションでVB-CABLEを選択できること
- OBS Browser Sourceで透明背景のAlphaが維持されること
- クロマキー緑でモデル輪郭に色かぶりがないこと
- 30分以上の連続動作で姿勢、メモリ、音声同期が悪化しないこと
- 大容量VRM・全VRMAを含む`.neonpack`の書き出しと別PCでの読み込み
- 自動画質がOBSとの同時利用時に過剰に上下しないこと

RDPセッションでは対象PC内のVB-CABLEがブラウザーへ公開されない場合があるため、音声系の最終確認はローカルコンソールで行う。

## 15. 基準版の判断

本仕様書作成時点では、以下を基準版とする。

- VRM・VRMAの読み込みが動作する
- VRMAクロスフェードが動作する
- 音声入力とリップシンクをVRMAより優先適用する
- 簡易VRMAの30fps補間と関節遅延が適用済み
- VRM・VRMA・主要設定の自動復元が動作する
- モデル別プロファイルとJSON設定移行が動作する
- 起動バッチからサーバーとブラウザーを起動できる
- ブルー、透明、クロマキー背景と配信表示を利用できる
- 視線は目を先行させ、頭・首を遅れて追従させる
- マイクロサッカード、自然瞬き、微表情、重心移動、不規則呼吸、抑揚うなずきを適用する
- VRMA再生後に腰ルートドリフトと足裏高を考慮した床面位置を補正する
- 自動画質と性能表示を利用できる
- 毎フレームのFFT配列と足位置Vector3を再利用し、GC起因の微小停止を抑制する
- VRMAの時間、ループ形式、再生中スロットを確認できる
- 右クリック確認操作でVRMAを個別解除できる
- `.neonpack`でVRM、VRMA、設定、全プロファイルを一括移行できる
- OBS用URLパラメーターで配信表示、背景、品質を指定できる
- 設定画面の文字色はダークブルー背景上で高コントラスト化済み

## 16. 現在の完成範囲

### 16.1 完成扱い

- ローカルNode.jsサーバーと起動バッチ
- VRM 0.x / 1.0読み込み、交換、自動復元
- 7スロットのVRMA読み込み、復元、解除
- VRMAクロスフェード、位相維持、旧アクション停止
- 発話状態によるIdle、Talk、TalkAlt、Emphasis自動選択
- 音声入力、自動ノイズ基準、母音リップシンク、子音閉口
- リップシンク遅延
- 自然瞬き、二度瞬き、マイクロサッカード
- 微表情、重心移動、不規則呼吸、抑揚うなずき
- 足裏高を考慮した安全な腰Y接地補正
- カメラ構図、照明、人間化プリセット
- モデル別プロファイル
- GTPLiveブルー、透明、クロマキー緑背景
- OBS配信表示とURLパラメーター
- 描画品質、自動画質、性能表示
- 設定JSONおよびNEON環境パック
- 開発仕様書とREADMEの運用説明
- Idle修正版は全トラックで先頭・末尾姿勢差0、開始・終了速度差0を確認済み

### 16.2 意図的に対象外

- Whisper、llama.cpp、TTSを使う音声会話
- 外部APIまたはクラウド送信
- 音声録音と文字起こし
- 写真版アバターの追加開発
- 骨軸を無視した汎用脚IK

### 16.3 次回再開時の推奨順序

1. ローカルコンソール上でVB-CABLEとOBSの実機確認
2. 保存データ管理画面
3. VRMAスロット別設定
4. 長時間安定性テスト
5. 必要なモデルに限定した脚IKの調査
